import React from "react";
import "./NumPlaySelector.scss";

/*
 * data: number of plays selection data
 * setData: useState
 */

export default function NumPlaySelector({ data, setData }) {
  console.log(data, setData);
  return <div>NumPlaySelector</div>;
}
